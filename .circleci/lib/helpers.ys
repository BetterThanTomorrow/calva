!yamlscript/v0

defn bin:
  (label name)::
    run:
      name:: label
      command::
        slurp: "bin/$name.bash"

  (label):
    name =: label.str/lower-case()
            .str/replace(' ', '-')
    bin: label name

defn cmd([label code])::
  run:
    name:: label
    command:: code
