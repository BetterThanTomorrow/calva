(defn foo [a b]
   (let [x + a b])
   (println "sum is" x))

     (defn foo [a b]
(let [x + a b])
  (println "sum is" x))

ddkjdkjk kj (defn foo [a b]
   (let [x + a b])
 (println "sum is" x))  (defn foo [a b]
(let [x + a b])
  (println "sum is" x))
  