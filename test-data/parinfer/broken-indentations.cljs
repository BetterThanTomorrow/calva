(ns foo
  (:require [clojure.string :refer [blank?
                                    lower-case
                                    split
                                  trim]])) ;; note: incorrect indentation

(def foo)